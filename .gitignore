dist/
.astro/
node_modules/

# Shared components, fetched at build time — never vendored.
_198x-ui/

# Synced house prose style, fetched by `vale sync` (see .vale.ini).
.vale/

# Playwright's own test artifacts.
test-results/
playwright-report/
# Copied out of the kit by scripts/fetch-ui.sh.
public/fonts/
